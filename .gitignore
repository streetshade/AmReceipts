node_modules
.next
out
build
dist

# env
.env
.env.local

# database
*.db
*.db-journal
prisma/dev.db
prisma/dev.db-journal

# uploads (receipt images stored on local disk in dev)
/public/uploads/*
!/public/uploads/.gitkeep

# misc
.DS_Store
*.log
next-env.d.ts
.vercel
*.tsbuildinfo

# Reference material and credentials must never enter this repository - it is
# public. Belt and braces: the intended home for M3 documentation is OUTSIDE
# the working tree entirely (see docs/M3_ION_INTEGRATION.md), but a stray copy
# dropped in here should not be committable by accident.
reference/
m3-reference/
*.ionapi
*.pfx
*.p12
